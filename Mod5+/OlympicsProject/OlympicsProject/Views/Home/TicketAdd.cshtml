@model Ticket
@{

}
<h2>New Task</h2>

<div asp-validation-summary="All" class="text-danger"></div>

<form asp-action="TicketAdd" method="post">
    <div class="mb-3">
        <label asp-for="Description" class="form-label">Description:</label>
        <input asp-for="Description" class="form-control">
    </div>

    <div class="mb-3">
        <label asp-for="Status" class="form-label">Status:</label>
        <select asp-for="Status" class="form-select" asp-items="@(new SelectList(ViewBag.Categories, "CategoryId", "CategoryName"))">
            <option value=""></option>
        </select>
    </div>

    <div class="mb-3">
        <label asp-for="DueDate" class="form-label">Due Date:</label>
        <input asp-for="DueDate" class="form-control" type="date">
    </div>
    
    <div class="mb-3">
        <label asp-for="StatusId" class="form-label">Status:</label>
        <select asp-for="StatusId" class="form-select" asp-items="@(new SelectList(ViewBag.Statuses, "StatusId", "StatusName"))"></select>
    </div>

    <button type="submit" class="btn btn-primary">Add</button>
    <a asp-action="Ticket" class="btn btn-danger">Cancel</a>
</form>
